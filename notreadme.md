# 기획
- 저장된 파일 구조를 볼 수 있는 ui 필요 API 포함
- 저장된 파일 이름 변경
- 폴더 생성
- 파일 기본 경로
  - /files/userid/foldersOrFiles
- 파일 암호화? 보류
- 같은 이름의 파일이 존재하는지 확인 필요 *****
  - 안하면 아무런 확인 없이 덮어써버림

## 도메인
- 스토리지
  - 저장
  - 조회
  - 삭제
  - 수정
  - 암호화-> 일단 보류
  - presigned URL -> 보류
    - 도입시 구조
      - 발급 후 클라이언트 업로드
      - 업로드 완료 후 메타데이터 DB 저장 요청

- 유틸
  - 파일 아이디 빌더

- 메타데이터
  - 스토리지 경로 저장
  - 사용자 아이디 기반으로 경로 조회
  - 공유등의 권한 관리


## 고민
- minio, s3에는 디렉토리 변경 기능이 없음
  - 디렉토리가 없기 떄문
    - 따라서 스토리지경로를 UI에 그대로 보여줄 수 없음
      - 파일을 폴더로 정리하고 이름을 바꾸는 기능이 필요하기 떄문
1. 스토리지에는 무작위 이름으로 저장하고 메타 DB에 매핑
   - hls가 고장남
   - 파일 이름을 건드리는건 안됨
2. 사용자명/날짜/uuid/파일
   - 폴더로 업로드 된 경우 하나의 uuid에 여러 파일 저장
   - 경로 길이가 길어져서 인덱스 용량이 증가할거임
   - hls안건들면서 중복 없이 나눠서 저장하는건 이게 최선인듯
   - 현재는 개별 요청이라 업로드 할 때마다 새로운 uuid가 생성 되는데 uuid그룹 만들기 기능 있어야할듯
3. 위 내용은 스토리지에 어떻게 저장할지고 ui에서 어떻게 폴더 변경 이름 변경등을 구현할지
   - 클로저 테이블
     - 구현 복잡함
   - 셀프 조인 테이블 -> 이걸로
     - 직박구리를 부모로 가지는 테이블 검색
       - 무당벌레 두견이

- 깊이가 깊은 경우 어떻게 해결하지
  - dir
    - dir2
      - dir3
        - dir4
  - dir을 그대로 드래그 드랍 하면 하위 디렉토리 전부 들어가야함
 - 어쩔수가 없다
   - dir
     - dir + dir2
       - dir + dir2 + dir3
   - 위같은 형식으로 저장 해야할듯
     - 안그럼 dir이름이 중복 된 상황 대처가 어려움
     - 디렉토리 이름 변경 대처 어려움
   - 테이블을 분리 해놓고 id로만 관리 한다면??
     - 있는지/없는지/모르는/디렉토리/파일이름
     - 일단 다 자르고 
       - 파일 구조 만들기
         - 만약 소유자 + 디렉토리 이름이 중복되면 제외 없으면 생성
     - 있는지
       - 없는지
         - 모르는
           - 디렉토리
             - 파일이름
             - 파일이름2
   - 
- 우선 뒤에서 잘라서 파일 이름을 추출
  - 0번째는 dir이름 1번째는 파일 이름이 되도록
  - parentDir에 0번 fileName에 1번